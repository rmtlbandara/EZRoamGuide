{
  "name": "ez-roam-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.10.1"
  },
  "devDependencies": {
    "wrangler": "4.104.0"
  }
}
